package com.michaelmidura.kataday.day009;

public class Kata {

    public static int travel(int totalTime, int runTime, int restTime, int speed) {
        //return ((totalTime / (runTime + restTime)) * speed * runTime + ((totalTime % (runTime + restTime)) != 0 ? ((totalTime >= runTime) ? (runTime * speed) : totalTime * speed) : 0));
        return 0;
    }
}